## 1. Activation Continuity

- [x] 1.1 在文档末尾手动激活结构化表格时，自动在表格下方保留普通文本空行作为出口
- [x] 1.2 实现首次激活时自动补齐首个空数据行，并把光标放到第一行第一列 cell 的文本光标位置
- [x] 1.3 收敛文档末尾首次激活路径，确保“首个空数据行”和“尾随空行出口”可以同时成立且不丢失光标
- [x] 1.4 明确定义并实现 `手动输入` 触发边界，只让逐字符跨过合法表格阈值的路径触发自动补齐，排除完整表格粘贴、撤销重放和程序化写入
- [x] 1.5 收敛首次激活的撤销边界，确保触发表格成立的最后一个输入字符与系统补齐共享同一个撤销语义

## 2. Terminal Table Deletion Guard

- [x] 2.1 为终端表格删除守卫增加显式的整表选中状态，并限制其只服务于文档末尾尾随空行删除场景
- [x] 2.2 拦截文档最后一行空行行首的后退删除：第一次删除选中上方整张表，而不是直接删除连接换行
- [x] 2.3 为整表选中补齐退出语义：点击其它位置、按方向键或继续输入时退出暂态，并回到锚点光标位置的正常编辑路径
- [x] 2.4 在整表已选中的前提下，第二次后退删除移除整张表，光标停在表格上方一行，并保持其它非守卫场景的普通删除语义不变
- [x] 2.5 补齐整表删除后的撤销恢复，确保表格与其下方普通文本空行一起恢复，且光标回到尾随空行行首

## 3. Verification

- [x] 3.1 补充自动化测试，覆盖手动录入到合法表格阈值时自动补齐首个空数据行、光标落到首个 cell，以及非终端激活不追加尾随空行
- [x] 3.2 补充自动化测试，覆盖文档末尾首次激活时“首个空数据行 + 尾随空行出口”同时成立
- [x] 3.3 补充自动化测试，覆盖首次激活补齐与触发字符共享同一个撤销语义，以及完整表格粘贴不触发额外补齐
- [x] 3.4 补充自动化测试与 `/sandbox` 手工验证，覆盖文档末尾尾随空行的两段式整表删除守卫、整表选中的退出路径，以及整表删除后的撤销恢复
