## 1. Hybrid 模式基础接入

- [x] 1.1 为 `UnaEditor` 增加 hybrid Markdown 渲染模式的公开配置项，并保持默认关闭
- [x] 1.2 重构 `useEditor` 的扩展组装逻辑，为 hybrid 渲染预留独立扩展挂载点
- [x] 1.3 为 hybrid 模式补充基础样式契约和命名约定，避免与现有编辑器样式冲突

## 2. 轻装饰层实现

- [x] 2.1 实现标题、强调、加粗、链接、行内代码等首期支持结构的装饰层渲染
- [x] 2.2 实现基于光标所在结构的源码态/渲染态切换规则，确保进入结构时恢复源码显示
- [x] 2.3 为 blockquote 和 fenced code block 添加首期保守视觉增强，并保持源码直接可编辑
- [x] 2.4 确认 Markdown 表格在 hybrid 模式下继续保持源码显示，不进入替换渲染路径

## 3. 图片渲染与性能控制

- [x] 3.1 实现 Markdown 图片语法的渲染层，在非激活状态下显示图片，在激活状态下恢复源码
- [x] 3.2 为图片渲染引入稳定的 widget 等价判断，减少无效销毁和重建
- [x] 3.3 实现边界驱动的更新策略，仅在光标跨越渲染单元边界时切换显示态
- [x] 3.4 将重渲染路径限制在可见区及其缓冲范围内，避免长文档下的高成本全量重算

## 4. 验证与示例

- [x] 4.1 为 hybrid 模式补充单元测试，覆盖开关行为、行内结构切换、图片切换和表格保持源码的场景
- [x] 4.2 更新 Playground，提供 hybrid 模式演示和关键 Markdown 场景的手工验证入口
- [x] 4.3 更新组件使用文档，说明 hybrid 模式的配置方式、首期支持范围和已知限制
